<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>WHITE HACKER AI v4.2 | Cosmic Synaptic Predictor</title>
  <style>
    body {
      margin: 0;
      font-family: "Courier New", monospace;
      background-color: #000;
      color: #00ff9d;
      text-align: center;
      height: 100vh;
      overflow: hidden;
    }

    h1 {
      color: #ff004c;
      text-shadow: 0 0 10px #ff004c, 0 0 20px #ff004c;
      margin-top: 30px;
    }

    .terminal-box {
      width: 80%;
      max-width: 600px;
      background: rgba(0, 0, 0, 0.8);
      border: 2px solid #00ff9d;
      padding: 20px;
      border-radius: 10px;
      margin: 40px auto;
      box-shadow: 0 0 20px #00ff9d;
      text-align: left;
    }

    button {
      background-color: #00ff9d;
      color: #000;
      border: none;
      padding: 10px 20px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      border-radius: 5px;
      margin-top: 20px;
      transition: 0.2s;
    }

    button:hover {
      background-color: #ff004c;
      color: #fff;
      box-shadow: 0 0 15px #ff004c;
    }

    .result-line {
      color: #fff;
      font-size: 14px;
      margin-top: 10px;
    }

    .color-red { color: #ff004c; }
    .color-green { color: #00ff9d; }
    .color-violet { color: #a020f0; }

    .footer {
      position: absolute;
      bottom: 15px;
      width: 100%;
      text-align: center;
      font-size: 12px;
      color: #666;
    }

    .glitch {
      animation: glitch 1s infinite;
    }

    @keyframes glitch {
      0% { text-shadow: 2px 2px #ff004c, -2px -2px #00ff9d; }
      50% { text-shadow: -2px -2px #ff004c, 2px 2px #00ff9d; }
      100% { text-shadow: 2px 2px #ff004c, -2px -2px #00ff9d; }
    }
  </style>
</head>
<body>
  <h1 class="glitch">WHITE HACKER AI v4.2</h1>
  <div class="terminal-box" id="terminal">
    <p><strong>OWNER:</strong> WHITE HACKER AI</p>
    <p><strong>VERSION:</strong> 4.2 "COSMIC SYNAPTIC CORE"</p>
    <p><strong>AUTHORIZATION:</strong> LEVEL OMEGA</p>
    <hr />
    <div id="output">
      <p>⚡ Ready for prediction...</p>
    </div>
    <button onclick="generatePrediction()">▶️ Next Prediction</button>
  </div>
  <div class="footer">© 2025 Cosmic Synaptic Core | AI Predictor Interface</div>

  <script>
    function generatePrediction() {
      const output = document.getElementById("output");
      const number = Math.floor(Math.random() * 10);
      const color = (number === 0 || number === 5)
        ? "🟣 Violet"
        : (number % 2 === 0 ? "🔴 Red" : "🟢 Green");
      const size = number <= 4 ? "SMALL" : "BIG";
      const confidence = (70 + Math.floor(Math.random() * 30));

      const colorClass =
        color.includes("Red") ? "color-red"
        : color.includes("Green") ? "color-green"
        : "color-violet";

      const html = `
        <p>=================================</p>
        <p>ANALYSIS: COMPLETE | PID: ${Math.floor(Math.random() * 999999)}</p>
        <p>▶️ PRIMARY PREDICTION</p>
        <p>- SIZE: <strong>${size}</strong></p>
        <p>- COLOR: <strong class="${colorClass}">${color}</strong></p>
        <p>- NUMBER: <strong>${number}</strong></p>
        <p>🔥 CONFIDENCE SCORE: ${confidence}%</p>
        <p>💡 DOMINANT LOGIC: Cosmic Pattern Sync</p>
        <p>=================================</p>
      `;
      output.innerHTML = html;
    }
  </script>
</body>
</html>
